# TypeScript packages

This directory is reserved for future independently versioned TypeScript
packages. SugarCode 3.0 keeps its private runtime protocol and shared UI types
inside `apps/desktop`; there is no generated app-server protocol package.
